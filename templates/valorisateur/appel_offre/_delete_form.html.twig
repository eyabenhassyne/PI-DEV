<div class="card border-danger shadow-sm mt-4">

  <div class="card-header bg-danger bg-opacity-10 text-danger fw-bold d-flex align-items-center gap-2">
    <i class="bi bi-exclamation-triangle-fill"></i>
    Zone dangereuse
  </div>

  <div class="card-body">

    <div class="alert alert-danger d-flex align-items-start gap-3 mb-4">
      <i class="bi bi-exclamation-octagon-fill fs-4"></i>
      <div>
        <strong>Attention :</strong><br>
        Cette action est <strong>irréversible</strong>.<br>
        L’appel sera définitivement supprimé de la base de données.
      </div>
    </div>

    <div class="d-flex justify-content-between align-items-center flex-wrap gap-3">

      <div class="text-muted small">
        Cette opération ne peut pas être annulée.
      </div>

      <form method="post"
            action="{{ path('val_appel_delete', {id: appel.id}) }}"
            onsubmit="return confirm('Confirmer la suppression définitive de cet appel ?');">

        <input type="hidden"
               name="_token"
               value="{{ csrf_token('delete_appel_' ~ appel.id) }}">

        <button class="btn btn-danger">
          <i class="bi bi-trash3-fill me-2"></i>
          Supprimer définitivement
        </button>

      </form>

    </div>

  </div>
</div>
