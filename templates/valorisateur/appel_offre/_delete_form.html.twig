<div class="card shadow-sm border-danger mt-4">

  <div class="card-header bg-danger text-white fw-bold d-flex align-items-center gap-2">
    <i class="bi bi-exclamation-triangle-fill"></i>
    Zone dangereuse
  </div>

  <div class="card-body">

    <div class="alert alert-danger d-flex align-items-start gap-3 mb-4">
      <i class="bi bi-exclamation-octagon-fill fs-4"></i>
      <div>
        <div class="fw-bold">Attention !</div>
        Cette action est <strong>irréversible</strong>.<br>
        L’appel sera supprimé définitivement de la base de données.
      </div>
    </div>

    <div class="d-flex justify-content-between align-items-center flex-wrap gap-3">

      <small class="text-muted">
        Cette opération ne peut pas être annulée.
      </small>

      <form method="post"
            action="{{ path('val_appel_delete', {id: appel.id}) }}"
            onsubmit="return confirm('Confirmer la suppression définitive de cet appel ?');"
            class="m-0">

        <input type="hidden"
               name="_token"
               value="{{ csrf_token('delete_appel_' ~ appel.id) }}">

        <button class="btn btn-danger">
          <i class="bi bi-trash3-fill me-2"></i>
          Supprimer définitivement
        </button>

      </form>

    </div>

  </div>
</div>
