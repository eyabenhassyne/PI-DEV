{% extends 'valorisateur/base_valorizateur.html.twig' %}

{% block title %}Détails appel | WasteWise TN{% endblock %}

{% block body %}

  {# sécurité: si statut null #}
  {% set st = appel.statut|default('BROUILLON') %}

  {# badge bootstrap #}
  {% set badgeClass =
    st == 'PUBLIE' ? 'success' :
    (st == 'BROUILLON' ? 'warning' :
      (st == 'FERME' ? 'secondary' : 'dark'))
  %}

  <div class="d-flex flex-column flex-md-row align-items-start align-items-md-center justify-content-between gap-2 mb-3">
    <div class="min-w-0">
      <h3 class="mb-1 text-truncate">📌 {{ appel.titre }}</h3>

      <div class="d-flex align-items-center gap-2 flex-wrap">
        <span class="badge text-bg-{{ badgeClass }}">
          {{ st }}
        </span>

        <span class="text-muted small">
          <i class="bi bi-calendar-event"></i>
          {{ appel.dateDebut ? appel.dateDebut|date('d/m/Y') : '—' }}
          →
          {{ appel.dateFin ? appel.dateFin|date('d/m/Y') : '—' }}
        </span>
      </div>
    </div>

    <div class="d-flex gap-2 flex-wrap">
      <a class="btn btn-outline-secondary" href="{{ path('val_appel_index') }}">
        <i class="bi bi-arrow-left"></i> Liste
      </a>

      <a class="btn btn-outline-primary" href="{{ path('val_appel_edit', {id: appel.id}) }}">
        <i class="bi bi-pencil"></i> Modifier
      </a>
    </div>
  </div>

  {# ===== DETAILS ===== #}
  <div class="card shadow-sm mb-3">
    <div class="card-header fw-bold d-flex align-items-center justify-content-between flex-wrap gap-2">
      <span>Détails</span>
      <span class="text-muted small">ID #{{ appel.id }}</span>
    </div>

    <div class="card-body">
      <div class="row g-3">
        <div class="col-12 col-md-6">
          <div class="text-muted small mb-1">Date début</div>
          <div class="fw-semibold">
            {{ appel.dateDebut ? appel.dateDebut|date('d/m/Y') : '—' }}
          </div>
        </div>

        <div class="col-12 col-md-6">
          <div class="text-muted small mb-1">Date fin</div>
          <div class="fw-semibold">
            {{ appel.dateFin ? appel.dateFin|date('d/m/Y') : '—' }}
          </div>
        </div>

        <div class="col-12">
          <div class="text-muted small mb-1">Description</div>
          <div class="border rounded-3 p-3 bg-light">
            {% if appel.description %}
              {{ appel.description }}
            {% else %}
              <span class="text-muted">—</span>
            {% endif %}
          </div>
        </div>
      </div>
    </div>
  </div>

  {# ===== ZONE DANGEREUSE ===== #}
  <div class="card border-danger shadow-sm">
    <div class="card-header bg-danger text-white fw-bold d-flex align-items-center gap-2">
      <i class="bi bi-exclamation-triangle-fill"></i> Zone dangereuse
    </div>

    <div class="card-body">
      <div class="alert alert-danger mb-3">
        <div class="fw-bold mb-1">Suppression définitive</div>
        <div class="mb-0">
          Cette action est irréversible. L’appel sera définitivement supprimé.
        </div>
      </div>

      <form method="post"
            action="{{ path('val_appel_delete', {id: appel.id}) }}"
            onsubmit="return confirm('Supprimer définitivement cet appel (#{{ appel.id }}) ?');"
            class="d-inline">

        <input type="hidden"
               name="_token"
               value="{{ csrf_token('delete_appel_' ~ appel.id) }}">

        <button class="btn btn-danger">
          <i class="bi bi-trash"></i> Supprimer définitivement
        </button>
      </form>
    </div>
  </div>

{% endblock %}
