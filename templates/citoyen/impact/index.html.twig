{# templates/citoyen/impact.html.twig #}
{% extends 'citoyen/base.html.twig' %}

{% block title %}Impact environnemental | WasteWise TN{% endblock %}

{% block body %}

<div class="container-fluid p-0">

  <div class="d-flex flex-column flex-md-row align-items-start align-items-md-center justify-content-between gap-3 mb-3">
    <div>
      <h3 class="mb-1">🌍 Impact environnemental</h3>
      <div class="text-muted">Basé uniquement sur les déclarations <b>VALIDÉES</b>.</div>
    </div>

    <div class="d-flex gap-2">
      <a class="btn btn-outline-secondary" href="{{ path('app_dashboard_citoyen') }}">
        <i class="bi bi-arrow-left"></i> Dashboard
      </a>
    </div>
  </div>

  {% set impactData = impact|default({}) %}
  {% set kgTotal = impactData.kgTotalValide|default(0) %}
  {% set kgByType = impactData.kgByType|default([]) %}
  {% set monthly  = impactData.monthly|default([]) %}

  {# ===== KPIs ===== #}
  <div class="row g-3 mb-3">
    <div class="col-12 col-md-4">
      <div class="card shadow-sm h-100">
        <div class="card-body">
          <div class="text-muted small">Total recyclé</div>
          <div class="fs-2 fw-bold">{{ kgTotal|number_format(2, '.', ' ') }} kg</div>
          <div class="text-muted small">Somme des quantités validées</div>
        </div>
      </div>
    </div>

    <div class="col-12 col-md-4">
      <div class="card shadow-sm h-100">
        <div class="card-body">
          <div class="text-muted small">Nombre de types</div>
          <div class="fs-2 fw-bold">{{ kgByType|length }}</div>
          <div class="text-muted small">Types distincts validés</div>
        </div>
      </div>
    </div>

    <div class="col-12 col-md-4">
      <div class="card shadow-sm h-100">
        <div class="card-body">
          <div class="text-muted small">Derniers mois</div>
          <div class="fs-2 fw-bold">{{ monthly|length }}</div>
          <div class="text-muted small">Données d'évolution disponibles</div>
        </div>
      </div>
    </div>
  </div>

  {# ===== Kg par type ===== #}
  <div class="card shadow-sm mb-3">
    <div class="card-header fw-bold">Kg par type</div>
    <div class="card-body">

      {% if kgByType is empty %}
        <div class="text-muted">Aucune déclaration validée pour le moment.</div>
      {% else %}
        {% set maxKg = (kgByType|first).kg|default(0) %}

        <div class="list-group">
          {% for row in kgByType %}
            {% set pct = (maxKg > 0) ? ((row.kg / maxKg) * 100) : 0 %}

            <div class="list-group-item">
              <div class="d-flex align-items-start justify-content-between gap-2">
                <div class="fw-semibold">{{ row.type }}</div>
                <div class="text-muted small">
                  <b class="text-dark">{{ row.kg|number_format(2, '.', ' ') }}</b> kg
                </div>
              </div>

              <div class="progress mt-2" style="height:8px;">
                <div class="progress-bar bg-success"
                     role="progressbar"
                     style="width: {{ pct|number_format(0, '.', '') }}%;"
                     aria-valuenow="{{ pct|number_format(0, '.', '') }}"
                     aria-valuemin="0"
                     aria-valuemax="100"></div>
              </div>
            </div>

          {% endfor %}
        </div>
      {% endif %}

    </div>
  </div>

  {# ===== Evolution mensuelle ===== #}
  <div class="card shadow-sm">
    <div class="card-header fw-bold">Évolution mensuelle (kg validés)</div>
    <div class="card-body">

      {% if monthly is empty %}
        <div class="text-muted">Aucune donnée d'évolution.</div>
      {% else %}
        <div class="table-responsive">
          <table class="table table-hover align-middle mb-0">
            <thead class="table-light">
              <tr>
                <th>Mois</th>
                <th>Total validé (kg)</th>
              </tr>
            </thead>
            <tbody>
              {% for m in monthly %}
                <tr>
                  <td><span class="badge text-bg-warning text-dark">{{ m.month }}</span></td>
                  <td class="fw-semibold">{{ m.kg|number_format(2, '.', ' ') }}</td>
                </tr>
              {% endfor %}
            </tbody>
          </table>
        </div>
      {% endif %}

    </div>
  </div>

</div>

{% endblock %}
