{# templates/citoyen/appel_show.html.twig #}
{% extends 'citoyen/base.html.twig' %}

{% block title %}Détails appel | WasteWise TN{% endblock %}

{% block body %}

<div class="container-fluid p-0">

  {# HEADER #}
  <div class="d-flex flex-column flex-md-row align-items-start align-items-md-center justify-content-between gap-3 mb-3">
    <div>
      <h3 class="mb-1">📣 {{ appel.titre }}</h3>
      <div class="text-muted">
        Du {{ appel.dateDebut ? appel.dateDebut|date('d/m/Y') : '—' }}
        au {{ appel.dateFin ? appel.dateFin|date('d/m/Y') : '—' }}
      </div>
    </div>

    <div class="d-flex align-items-center gap-2 flex-wrap">
      {# Badge statut : BROUILLON / PUBLIE / FERME #}
      {% set st = appel.statut|default('BROUILLON') %}
      {% set badgeClass =
        st == 'PUBLIE' ? 'text-bg-success' :
        (st == 'FERME' ? 'text-bg-secondary' : 'text-bg-warning')
      %}

      <span class="badge {{ badgeClass }}">{{ st }}</span>

      <a class="btn btn-outline-secondary" href="{{ path('cit_appel_index') }}">
        <i class="bi bi-arrow-left"></i> Retour
      </a>
    </div>
  </div>

  {# FLASH MESSAGES (tous types) #}
  {% for label, messages in app.flashes %}
    {% for message in messages %}
      <div class="alert alert-{{ label == 'danger' ? 'danger' : (label == 'error' ? 'danger' : label) }} shadow-sm">
        {{ message }}
      </div>
    {% endfor %}
  {% endfor %}

  {# CARD DESCRIPTION #}
  <div class="card shadow-sm">
    <div class="card-header fw-bold">Description</div>
    <div class="card-body">

      <p class="text-muted mb-3" style="line-height:1.7;">
        {{ appel.description ?: '—' }}
      </p>

      <div class="d-flex justify-content-end gap-2 flex-wrap">
        {% if appel.isOuvert() %}
          <a class="btn btn-success"
             href="{{ path('cit_appel_proposer', {id: appel.id}) }}">
            <i class="bi bi-send"></i> Envoyer une proposition
          </a>
        {% else %}
          <button class="btn btn-secondary" disabled>
            <i class="bi bi-lock"></i> Appel non ouvert
          </button>
        {% endif %}
      </div>

    </div>
  </div>

</div>

{% endblock %}
