{% extends 'citoyen/base.html.twig' %}

{% block title %}Mes récompenses | WasteWise TN{% endblock %}

{% block body %}
<div class="container-fluid p-0">

  <div class="d-flex flex-column flex-md-row align-items-start align-items-md-center justify-content-between gap-2 mb-3">
    <div>
      <h3 class="mb-1">🏆 Mes récompenses acquises</h3>
      <div class="text-muted">Historique de tes échanges</div>
    </div>
    <div class="d-flex gap-2">
      <a class="btn btn-outline-secondary" href="{{ path('app_citoyen_recompenses') }}">
        <i class="bi bi-gift"></i> Voir les récompenses
      </a>
    </div>
  </div>

  <div class="card shadow-sm">
    <div class="card-header fw-bold">Liste</div>

    {% if items is empty %}
      <div class="card-body text-muted">Aucune récompense acquise pour le moment.</div>
    {% else %}
      <div class="table-responsive">
        <table class="table table-hover align-middle mb-0">
          <thead class="table-light">
            <tr>
              <th>Récompense</th>
              <th>Partenaire</th>
              <th>Points utilisés</th>
              <th>Date</th>
            </tr>
          </thead>
          <tbody>
          {% for e in items %}
            <tr>
              <td class="fw-semibold">{{ e.recompense.titre }}</td>
              <td>{{ e.recompense.partenaire ? e.recompense.partenaire.nomEntreprise : '—' }}</td>
              <td><span class="badge bg-success">{{ e.pointsUtilises }} pts</span></td>
              <td>{{ e.createdAt ? e.createdAt|date('d/m/Y H:i') : '—' }}</td>
            </tr>
          {% endfor %}
          </tbody>
        </table>
      </div>
    {% endif %}
  </div>

</div>
{% endblock %}
