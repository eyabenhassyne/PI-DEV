{% extends 'base.html.twig' %}

{% block title %}Connexion | WasteWise TN{% endblock %}

{% block stylesheets %}
<style>
  .auth-wrap{
    display:grid;
    grid-template-columns: 1.2fr 0.8fr;
    gap:18px;
    align-items:stretch;
    margin-top:18px;
  }
  @media(max-width: 980px){
    .auth-wrap{ grid-template-columns:1fr; }
  }

  .auth-hero{
    border-radius:22px;
    padding:24px;
    border:1px solid rgba(0,0,0,.06);
    background: linear-gradient(135deg, rgba(16,185,129,.14), rgba(34,197,94,.08));
    min-height: 460px;
    display:flex;
    flex-direction:column;
    justify-content:space-between;
  }

  .auth-hero h1{
    margin:0;
    font-size:38px;
    line-height:1.05;
    font-weight:900;
    letter-spacing:-.4px;
  }
  .auth-hero p{ margin:10px 0 0; color:rgba(0,0,0,.65); max-width:56ch; }

  .auth-badges{ display:grid; gap:10px; margin-top:16px; }
  .auth-badge{
    display:flex; gap:10px; align-items:flex-start;
    background: rgba(255,255,255,.75);
    border:1px solid rgba(0,0,0,.06);
    padding:12px 14px;
    border-radius:16px;
  }
  .auth-badge .ic{
    width:34px;height:34px;border-radius:12px;
    display:grid;place-items:center;
    background: rgba(16,185,129,.14);
  }

  .auth-card{
    border-radius:22px;
    background:#fff;
    border:1px solid rgba(0,0,0,.06);
    box-shadow: 0 12px 35px rgba(15,23,42,.08);
    padding:22px;
    min-height: 460px;
  }
  .auth-title{ margin:0; font-weight:900; font-size:26px; }
  .auth-sub{ margin:6px 0 0; color:rgba(0,0,0,.6); }

  .auth-field{ margin-top:14px; }
  .auth-field label{ display:block; font-weight:800; font-size:13px; margin-bottom:7px; }
  .auth-input{
    width:100%;
    padding:12px 14px;
    border-radius:14px;
    border:1px solid rgba(0,0,0,.10);
    outline:none;
    font-size:14px;
    background:#fff;
  }
  .auth-input:focus{
    border-color: rgba(16,185,129,.55);
    box-shadow: 0 0 0 4px rgba(16,185,129,.12);
  }

  .auth-row{
    display:flex;
    align-items:center;
    justify-content:space-between;
    gap:12px;
    margin-top:10px;
    flex-wrap:wrap;
    font-weight:700;
    font-size:13px;
    color:rgba(0,0,0,.65);
  }
  .auth-link{ color:#0f766e; text-decoration:none; font-weight:900; }
  .auth-link:hover{ text-decoration:underline; }

  .auth-btn{
    margin-top:14px;
    width:100%;
    padding:12px 14px;
    border:0;
    border-radius:14px;
    font-weight:900;
    cursor:pointer;
    background: linear-gradient(180deg, #22c55e, #16a34a);
    color:#fff;
  }

  .auth-alert{
    margin-top:12px;
    border-radius:14px;
    padding:10px 12px;
    border:1px solid rgba(239,68,68,.25);
    background: rgba(254,242,242,.9);
    color:#991b1b;
    font-weight:800;
    font-size:13px;
  }
</style>
{% endblock %}

{% block body %}
<div class="auth-wrap">

  <section class="auth-hero">
    <div>
      <h1>Bienvenue 👋</h1>
      <p>Connecte-toi pour déclarer tes déchets, suivre tes validations et gagner des EcoPoints.</p>

      <div class="auth-badges">
        <div class="auth-badge">
          <div class="ic">🗑️</div>
          <div>
            <div style="font-weight:900;">Déclaration simple</div>
            <div style="color:rgba(0,0,0,.6); font-size:13px;">Type, quantité, localisation</div>
          </div>
        </div>
        <div class="auth-badge">
          <div class="ic">✅</div>
          <div>
            <div style="font-weight:900;">Validation transparente</div>
            <div style="color:rgba(0,0,0,.6); font-size:13px;">En attente / validé / refusé</div>
          </div>
        </div>
        <div class="auth-badge">
          <div class="ic">🎁</div>
          <div>
            <div style="font-weight:900;">EcoPoints</div>
            <div style="color:rgba(0,0,0,.6); font-size:13px;">Récompenses et badges</div>
          </div>
        </div>
      </div>
    </div>

    <div style="color:rgba(0,0,0,.55); font-weight:800; font-size:13px;">
      WasteWise TN — Plateforme citoyenne 🌿
    </div>
  </section>

  <section class="auth-card">
    <h2 class="auth-title">Se connecter</h2>
    <p class="auth-sub">Accède à ton espace WasteWise TN</p>

    {% if error %}
      <div class="auth-alert">
        {{ error.messageKey|trans(error.messageData, 'security') }}
      </div>
    {% endif %}

    <form method="post">
      <div class="auth-field">
        <label for="inputEmail">Email</label>
        <input id="inputEmail" name="email" type="email"
               class="auth-input"
               value="{{ last_username }}"
               autocomplete="email" required autofocus>
      </div>

      <div class="auth-field">
        <label for="inputPassword">Mot de passe</label>
        <input id="inputPassword" name="password" type="password"
               class="auth-input"
               autocomplete="current-password" required>
      </div>

      <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">

      <div class="auth-row">
        <label style="display:flex; gap:8px; align-items:center;">
          <input type="checkbox" name="_remember_me">
          Se souvenir de moi
        </label>
        <a class="auth-link" href="#" onclick="alert('Bientôt disponible'); return false;">Mot de passe oublié ?</a>
      </div>

      <button class="auth-btn" type="submit">Connexion</button>

      <div class="auth-row" style="justify-content:flex-end;">
        <span>Pas de compte ?</span>
        <a class="auth-link" href="{{ path('app_register') }}">Créer un compte</a>
      </div>
    </form>
  </section>

</div>
{% endblock %}
