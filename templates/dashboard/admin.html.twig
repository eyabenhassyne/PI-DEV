{% extends 'admin/base_admin.html.twig' %}

{% block title %}Dashboard Admin | WasteWise{% endblock %}
{% block page_title %}Admin{% endblock %}

{% block sidebar %}
  {% include 'admin/_sidebar.html.twig' %}
{% endblock %}

{% block body %}

  {# ===== HERO ===== #}
  <div class="hero d-flex flex-column flex-md-row align-items-start align-items-md-center justify-content-between gap-3">
    <div>
      <h2 class="mb-1">Bienvenue Admin 👋</h2>
      <div class="opacity-75">
        Gestion des utilisateurs, partenaires, récompenses et campagnes
      </div>
    </div>

    <div class="d-flex gap-2 flex-wrap">
      <a class="btn btn-light" href="{{ path('app_user_index') }}">
        <i class="fa-solid fa-users me-2"></i> Utilisateurs
      </a>
      <a class="btn btn-light" href="{{ path('partenaire_index') }}">
        <i class="fa-solid fa-building me-2"></i> Partenaires
      </a>
      <a class="btn btn-light" href="{{ path('recompense_index') }}">
        <i class="fa-solid fa-gift me-2"></i> Récompenses
      </a>
      <a class="btn btn-light" href="{{ path('admin_campagne_index') }}">
        <i class="fa-solid fa-bullhorn me-2"></i> Campagnes
      </a>
    </div>
  </div>

  {# ===== KPIs ===== #}
  <div class="row g-4 mb-4">

    <div class="col-12 col-md-6 col-xl-3">
      <div class="stat-card h-100">
        <div class="d-flex align-items-start justify-content-between">
          <div>
            <div class="text-muted small">Utilisateurs</div>
            <div class="fs-2 fw-bold">{{ usersCount|default('—') }}</div>
            <div class="text-muted small">Comptes au total</div>
          </div>
          <div class="rounded-4 d-flex align-items-center justify-content-center"
               style="width:44px;height:44px;background:rgba(47,107,79,.12);color:#2f6b4f;">
            <i class="fa-solid fa-users"></i>
          </div>
        </div>
        <div class="mt-3">
          <a class="btn btn-sm btn-outline-success w-100" href="{{ path('app_user_index') }}">
            Gérer <i class="fa-solid fa-arrow-right ms-1"></i>
          </a>
        </div>
      </div>
    </div>

    <div class="col-12 col-md-6 col-xl-3">
      <div class="stat-card h-100">
        <div class="d-flex align-items-start justify-content-between">
          <div>
            <div class="text-muted small">Partenaires</div>
            <div class="fs-2 fw-bold">{{ partenairesCount|default('—') }}</div>
            <div class="text-muted small">Entreprises / ONG</div>
          </div>
          <div class="rounded-4 d-flex align-items-center justify-content-center"
               style="width:44px;height:44px;background:rgba(141,210,47,.18);color:#2f6b4f;">
            <i class="fa-solid fa-building"></i>
          </div>
        </div>
        <div class="mt-3">
          <a class="btn btn-sm btn-outline-success w-100" href="{{ path('partenaire_index') }}">
            Gérer <i class="fa-solid fa-arrow-right ms-1"></i>
          </a>
        </div>
      </div>
    </div>

    <div class="col-12 col-md-6 col-xl-3">
      <div class="stat-card h-100">
        <div class="d-flex align-items-start justify-content-between">
          <div>
            <div class="text-muted small">Récompenses</div>
            <div class="fs-2 fw-bold">{{ recompensesCount|default('—') }}</div>
            <div class="text-muted small">Catalogue</div>
          </div>
          <div class="rounded-4 d-flex align-items-center justify-content-center"
               style="width:44px;height:44px;background:rgba(59,130,246,.12);color:#2563eb;">
            <i class="fa-solid fa-gift"></i>
          </div>
        </div>
        <div class="mt-3">
          <a class="btn btn-sm btn-outline-primary w-100" href="{{ path('recompense_index') }}">
            Gérer <i class="fa-solid fa-arrow-right ms-1"></i>
          </a>
        </div>
      </div>
    </div>

    <div class="col-12 col-md-6 col-xl-3">
      <div class="stat-card h-100">
        <div class="d-flex align-items-start justify-content-between">
          <div>
            <div class="text-muted small">Campagnes</div>
            <div class="fs-2 fw-bold">{{ campagnesCount|default('—') }}</div>
            <div class="text-muted small">Actives / terminées</div>
          </div>
          <div class="rounded-4 d-flex align-items-center justify-content-center"
               style="width:44px;height:44px;background:rgba(245,158,11,.16);color:#b45309;">
            <i class="fa-solid fa-bullhorn"></i>
          </div>
        </div>
        <div class="mt-3">
          <a class="btn btn-sm btn-outline-warning w-100" href="{{ path('admin_campagne_index') }}">
            Gérer <i class="fa-solid fa-arrow-right ms-1"></i>
          </a>
        </div>
      </div>
    </div>

  </div>

  {# ===== CONTENU / BLOCS ===== #}
  <div class="row g-4">
    <div class="col-12 col-lg-7">
      <div class="big-card h-100">
        <div class="d-flex align-items-center justify-content-between mb-2">
          <h5 class="mb-0">📊 Vue rapide</h5>
          <span class="badge bg-light text-dark border">
            {{ "now"|date("d/m/Y") }}
          </span>
        </div>
        <p class="text-muted mb-0">
          Ici tu peux ajouter des graphiques (EcoPoints distribués, déclarations validées, top partenaires, etc.)
        </p>

        <hr class="my-3">

        <div class="row g-3">
          <div class="col-md-6">
            <div class="p-3 rounded-4 border bg-light">
              <div class="fw-semibold mb-1">💡 Idée</div>
              <div class="text-muted small">
                Top 5 partenaires (récompenses les plus échangées)
              </div>
            </div>
          </div>
          <div class="col-md-6">
            <div class="p-3 rounded-4 border bg-light">
              <div class="fw-semibold mb-1">✅ Idée</div>
              <div class="text-muted small">
                Déclarations: en attente / validées / refusées
              </div>
            </div>
          </div>
        </div>

      </div>
    </div>

    <div class="col-12 col-lg-5">
      <div class="big-card h-100">
        <h5 class="mb-3">⚡ Raccourcis</h5>

        <div class="d-grid gap-2">
          <a class="btn btn-outline-success" href="{{ path('app_user_new') }}">
            <i class="fa-solid fa-user-plus me-2"></i> Créer un utilisateur
          </a>
          <a class="btn btn-outline-success" href="{{ path('partenaire_new') }}">
            <i class="fa-solid fa-plus me-2"></i> Nouveau partenaire
          </a>
          <a class="btn btn-outline-primary" href="{{ path('recompense_new') }}">
            <i class="fa-solid fa-gift me-2"></i> Nouvelle récompense
          </a>
          <a class="btn btn-outline-warning" href="{{ path('admin_campagne_new') }}">
            <i class="fa-solid fa-bullhorn me-2"></i> Nouvelle campagne
          </a>
        </div>

        <hr class="my-3">

        <div class="text-muted small">
          Astuce : garde ce bloc pour les actions les plus fréquentes.
        </div>
      </div>
    </div>
  </div>

{% endblock %}
