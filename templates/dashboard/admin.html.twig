{% extends 'admin/base_admin.html.twig' %}

{% block title %}Dashboard Admin | WasteWise TN{% endblock %}

{% block body %}

<div class="container-fluid py-4">

  <div class="d-flex align-items-start justify-content-between mb-4">
    <div>
      <h3 class="mb-1">📊 Dashboard Administrateur</h3>
      <div class="text-muted">Vue globale de la plateforme</div>
    </div>
  </div>

  <div class="row g-3 mb-3">

    <div class="col-12 col-md-6 col-xl-4">
      <a href="{{ path('app_user_index') }}" class="text-decoration-none">
        <div class="card shadow-sm h-100">
          <div class="card-body">
            <div class="text-muted mb-1">Utilisateurs</div>
            <div class="fs-2 fw-bold">{{ usersTotal ?? 0 }}</div>
            <div class="small text-muted">Gérer les comptes</div>
          </div>
        </div>
      </a>
    </div>

    <div class="col-12 col-md-6 col-xl-4">
      <div class="card shadow-sm h-100">
        <div class="card-body">
          <div class="text-muted mb-1">Déclarations</div>
          <div class="fs-2 fw-bold">{{ dechetsTotal ?? 0 }}</div>
          <div class="small text-muted">Total déclaré</div>
        </div>
      </div>
    </div>

    <div class="col-12 col-md-6 col-xl-4">
      <div class="card shadow-sm h-100">
        <div class="card-body">
          <div class="text-muted mb-1">En attente</div>
          <div class="fs-2 fw-bold">{{ enAttente ?? 0 }}</div>
          <div class="small text-muted">À traiter</div>
        </div>
      </div>
    </div>

  </div>

  <div class="row g-3 mb-4">
    <div class="col-12 col-md-6 col-xl-4">
      <div class="card shadow-sm h-100">
        <div class="card-body">
          <div class="text-muted mb-1">Validées</div>
          <div class="fs-2 fw-bold">{{ valides ?? 0 }}</div>
        </div>
      </div>
    </div>

    <div class="col-12 col-md-6 col-xl-4">
      <div class="card shadow-sm h-100">
        <div class="card-body">
          <div class="text-muted mb-1">Refusées</div>
          <div class="fs-2 fw-bold">{{ refuses ?? 0 }}</div>
        </div>
      </div>
    </div>

    <div class="col-12 col-md-6 col-xl-4">
      <a href="{{ path('admin_campagne_index') }}" class="text-decoration-none">
        <div class="card shadow-sm h-100">
          <div class="card-body">
            <div class="text-muted mb-1">Campagnes</div>
            <div class="fs-2 fw-bold">{{ campagnesTotal ?? 0 }}</div>
            <div class="small text-muted">Voir / gérer</div>
          </div>
        </div>
      </a>
    </div>
  </div>

  <div class="card shadow-sm mb-4">
    <div class="card-header fw-bold">Derniers utilisateurs</div>
    <div class="table-responsive">
      <table class="table table-hover align-middle mb-0">
        <thead class="table-light">
          <tr>
            <th>Email</th>
            <th>Type</th>
            <th>Date</th>
          </tr>
        </thead>
        <tbody>
          {% for u in lastUsers %}
            <tr>
              <td>{{ u.email }}</td>
              <td>{{ u.type ?? (u.roles is defined ? u.roles|join(', ') : '—') }}</td>
              <td>{{ u.createdAt ? u.createdAt|date('d/m/Y') : '—' }}</td>
            </tr>
          {% else %}
            <tr><td colspan="3" class="text-muted">Aucun utilisateur récent.</td></tr>
          {% endfor %}
        </tbody>
      </table>
    </div>
  </div>

  <div class="card shadow-sm">
    <div class="card-header fw-bold">Dernières déclarations</div>
    <div class="table-responsive">
      <table class="table table-hover align-middle mb-0">
        <thead class="table-light">
          <tr>
            <th>Type</th>
            <th>Quantité</th>
            <th>Statut</th>
            <th>Date</th>
          </tr>
        </thead>
        <tbody>
          {% for d in lastDechets %}
            <tr>
              <td>{{ d.type ?? d.typeDechet ?? d.categorie ?? '—' }}</td>
              <td>{{ d.quantiteKg ?? '—' }} kg</td>
              <td>{{ d.statut ?? '—' }}</td>
              <td>{{ d.createdAt ? d.createdAt|date('d/m/Y') : '—' }}</td>
            </tr>
          {% else %}
            <tr><td colspan="4" class="text-muted">Aucune déclaration récente.</td></tr>
          {% endfor %}
        </tbody>
      </table>
    </div>
  </div>

</div>

{% endblock %}
