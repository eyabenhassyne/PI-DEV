{# templates/citoyen/dashboard.html.twig #}
{% extends 'citoyen/base.html.twig' %}

{% block title %}Dashboard Citoyen | WasteWise TN{% endblock %}

{% block body %}

<div class="container-fluid p-0">

  <div class="d-flex align-items-start justify-content-between mb-4">
    <div>
      <h3 class="mb-1">🏠 Dashboard Citoyen</h3>
      <div class="text-muted">Vue globale de votre activité</div>
    </div>

    <div class="d-flex gap-2">
      <a class="btn btn-success" href="{{ path('dechet_nouveau') }}">
        <i class="bi bi-plus-circle"></i> Déclarer
      </a>
      <a class="btn btn-outline-secondary" href="{{ path('citoyen_declarations') }}">
        <i class="bi bi-file-earmark-text"></i> Mes déclarations
      </a>
    </div>
  </div>

  <div class="row g-3 mb-3">
    <div class="col-12 col-md-6 col-xl-4">
      <div class="card shadow-sm h-100">
        <div class="card-body">
          <div class="text-muted mb-1">EcoPoints</div>
          <div class="fs-2 fw-bold">{{ ecoPoints|default(0) }}</div>
          <div class="small text-muted">Après validation</div>
        </div>
      </div>
    </div>

    <div class="col-12 col-md-6 col-xl-4">
      <div class="card shadow-sm h-100">
        <div class="card-body">
          <div class="text-muted mb-1">Déclarations</div>
          <div class="fs-2 fw-bold">{{ stats.total|default(0) }}</div>
          <div class="small text-muted">
            {{ stats.enAttente|default(0) }} en attente •
            {{ stats.valides|default(0) }} validées •
            {{ stats.refuses|default(0) }} refusées
          </div>
        </div>
      </div>
    </div>

    <div class="col-12 col-md-6 col-xl-4">
      <div class="card shadow-sm h-100">
        <div class="card-body">
          <div class="text-muted mb-1">Badge</div>
          <div class="fs-2 fw-bold">{{ badge|default('Débutant') }}</div>
          <div class="small text-muted">Continue 💪</div>
        </div>
      </div>
    </div>
  </div>

  <div class="card shadow-sm">
    <div class="card-header fw-bold">Activité récente</div>
    <div class="table-responsive">
      <table class="table table-hover align-middle mb-0">
        <thead class="table-light">
          <tr>
            <th>Type</th>
            <th>Quantité</th>
            <th>Statut</th>
            <th>Date</th>
          </tr>
        </thead>
        <tbody>
          {% for d in recent|default([]) %}
            <tr>
              <td>{{ d.type ?? '—' }}</td>
              <td>{{ d.quantiteKg ?? '—' }} kg</td>
              <td>{{ d.statut ?? '—' }}</td>
              <td>{{ d.createdAt ? d.createdAt|date('d/m/Y H:i') : '—' }}</td>
            </tr>
          {% else %}
            <tr><td colspan="4" class="text-muted">Aucune déclaration récente.</td></tr>
          {% endfor %}
        </tbody>
      </table>
    </div>
  </div>

</div>

{% endblock %}
