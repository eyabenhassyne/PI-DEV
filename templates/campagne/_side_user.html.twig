<aside class="ww-side">
  <div class="ww-side-head">
    <div class="ww-side-logo">WW</div>
    <div>
      <div class="ww-side-brand">WasteWise <span>TN</span></div>
      <div class="ww-side-tag">Espace Utilisateur</div>
    </div>
  </div>

  <div class="ww-side-user">
    <div class="ww-avatar">{{ app.user ? app.user.email|first|upper : 'U' }}</div>
    <div>
      <div class="ww-user-email">{{ app.user ? app.user.email : 'Invité' }}</div>
      <div class="ww-user-meta">
        <span class="ww-pill ww-ok">USER</span>
      </div>
    </div>
  </div>

  {% set route = app.request.attributes.get('_route') %}

  <nav class="ww-side-nav">
    <a class="ww-nav-item {{ route == 'app_dashboard_citoyen' ? 'active' : '' }}" href="{{ path('app_dashboard_citoyen') }}">
      <span class="ww-nav-ic">🏠</span> Mon Dashboard
    </a>

    <a class="ww-nav-item {{ route starts with 'campagne_' and route != 'campagne_my' ? 'active' : '' }}" href="{{ path('campagne_index') }}">
      <span class="ww-nav-ic">📣</span> Campagnes
    </a>

    <a class="ww-nav-item {{ route == 'campagne_my' ? 'active' : '' }}" href="{{ path('campagne_my') }}">
      <span class="ww-nav-ic">✅</span> Mes campagnes
    </a>

    <div class="ww-nav-sep"></div>

    {% if app.user %}
      <a class="ww-nav-item danger" href="{{ path('app_logout') }}">
        <span class="ww-nav-ic">🚪</span> Déconnexion
      </a>
    {% else %}
      <a class="ww-nav-item" href="{{ path('app_login') }}">
        <span class="ww-nav-ic">🔑</span> Connexion
      </a>
    {% endif %}
  </nav>

  <div class="ww-side-foot">© {{ "now"|date("Y") }} WasteWise TN</div>
</aside>
