{# templates/citoyen/dechets.html.twig #}
{% extends 'citoyen/base.html.twig' %}

{% block title %}Déchets | WasteWise TN{% endblock %}

{% block body %}

<div class="container-fluid p-0">

  <div class="d-flex flex-column flex-md-row align-items-start align-items-md-center justify-content-between gap-3 mb-3">
    <div>
      <h3 class="mb-1">🗑️ Déchets</h3>
      <div class="text-muted">Page de test. Utilise le menu pour déclarer un déchet.</div>
    </div>

    <div class="d-flex gap-2 flex-wrap">
      <a class="btn btn-success" href="{{ path('dechet_nouveau') }}">
        <i class="bi bi-plus-circle"></i> Déclarer un déchet
      </a>
      <a class="btn btn-outline-secondary" href="{{ path('app_dashboard_citoyen') }}">
        <i class="bi bi-arrow-left"></i> Dashboard
      </a>
    </div>
  </div>

  <div class="card shadow-sm">
    <div class="card-body">
      <h5 class="mb-1">ℹ️ Info</h5>
      <p class="text-muted mb-0">
        Cette page est une page placeholder. Pour ajouter une déclaration,
        clique sur <b>“Déclarer un déchet”</b> ou utilise le menu à gauche.
      </p>
    </div>
  </div>

</div>

{% endblock %}
