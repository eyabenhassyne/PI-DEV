<form method="post" action="{{ path('app_appel_offre_delete', {'id': appel_offre.id}) }}" onsubmit="return confirm('Supprimer cet appel d\'offre ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ appel_offre.id) }}">
    <button class="ww-btn ww-btn--light">Supprimer</button>
</form>
