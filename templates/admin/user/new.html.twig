{% extends 'admin/base_admin.html.twig' %}

{% block title %}Créer utilisateur | WasteWise{% endblock %}
{% block page_title %}Utilisateurs{% endblock %}

{% block sidebar %}
  {% include 'admin/_sidebar.html.twig' %}
{% endblock %}

{% block body %}

<div class="d-flex align-items-center justify-content-between flex-wrap gap-2 mb-4">
  <div>
    <h2 class="mb-0">➕ Créer un utilisateur</h2>
    <div class="text-muted small">
      Ajouter un nouveau compte à la plateforme
    </div>
  </div>

  <a href="{{ path('app_user_index') }}" class="btn btn-outline-secondary">
    <i class="bi bi-arrow-left"></i> Retour à la liste
  </a>
</div>

<div class="row g-4">

  {# ================= FORMULAIRE ================= #}
  <div class="col-lg-8">
    <div class="card shadow-sm border-0">
      <div class="card-header bg-white d-flex align-items-center justify-content-between">
        <div class="fw-bold">
          <i class="bi bi-person-plus me-1"></i> Formulaire utilisateur
        </div>
        <span class="badge bg-success-subtle text-success border">
          Nouveau
        </span>
      </div>

      <div class="card-body">
        {# ✅ CHEMIN CORRIGÉ #}
        {{ include('admin/user/_form.html.twig', { button_label: 'Créer' }) }}
      </div>
    </div>
  </div>

  {# ================= CONSEILS ================= #}
  <div class="col-lg-4">
    <div class="card shadow-sm border-0">
      <div class="card-header bg-white fw-bold">
        <i class="bi bi-lightbulb me-1"></i> Conseils
      </div>

      <div class="card-body">
        <ul class="mb-0 small text-muted">
          <li>Vérifie que l’email n’existe pas déjà.</li>
          <li>Choisis le type adapté (ADMIN / VALORIZER / CITOYEN).</li>
          <li>Le mot de passe sera automatiquement hashé.</li>
          <li>Un utilisateur ADMIN aura accès complet.</li>
        </ul>
      </div>
    </div>
  </div>

</div>

{% endblock %}
