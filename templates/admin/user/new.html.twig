{% extends 'admin/base_admin.html.twig' %}

{% block title %}Créer utilisateur | WasteWise{% endblock %}

{% block body %}

<div class="d-flex align-items-center justify-content-between flex-wrap gap-2 mb-3">
  <div>
    <h2 class="mb-0">➕ Créer un utilisateur</h2>
    <div class="text-muted small">Ajouter un nouveau compte à la plateforme</div>
  </div>

  <a href="{{ path('app_user_index') }}" class="btn btn-outline-secondary">
    <i class="bi bi-arrow-left"></i> Retour à la liste
  </a>
</div>

<div class="row g-3">
  <div class="col-lg-8">

    <div class="card shadow-sm border-0">
      <div class="card-header bg-white d-flex align-items-center justify-content-between">
        <div class="fw-bold">Formulaire utilisateur</div>
        <span class="badge bg-light text-dark border">
          <i class="bi bi-person-plus"></i> Nouveau
        </span>
      </div>

      <div class="card-body">
        {{ include('user/_form.html.twig', { button_label: 'Créer' }) }}
      </div>
    </div>

  </div>

  <div class="col-lg-4">

    <div class="card shadow-sm border-0">
      <div class="card-header bg-white fw-bold">
        Conseils
      </div>

      <div class="card-body">
        <ul class="mb-0 small text-muted">
          <li>Vérifie l’email avant de créer.</li>
          <li>Choisis le rôle adapté (ADMIN / VALORIZER / CITOYEN).</li>
          <li>Le mot de passe sera hashé automatiquement si tu l’as prévu dans le controller.</li>
        </ul>
      </div>
    </div>

  </div>
</div>

{% endblock %}
